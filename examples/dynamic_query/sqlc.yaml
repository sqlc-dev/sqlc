version: "2"
sql:
  - engine: "postgresql"
    schema: "postgresql/schema.sql"
    queries: "postgresql/query.sql"
    gen:
      go:
        package: "postgresql"
        out: "postgresql"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: true
