// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: query.sql

package querytest

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const unionAllBothNotNull = `-- name: UnionAllBothNotNull :many
SELECT id, label, rank FROM not_null_leg
UNION ALL
SELECT id, label, rank FROM not_null_leg
`

// Both legs NOT NULL — result stays NOT NULL.
func (q *Queries) UnionAllBothNotNull(ctx context.Context) ([]NotNullLeg, error) {
	rows, err := q.db.Query(ctx, unionAllBothNotNull)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []NotNullLeg
	for rows.Next() {
		var i NotNullLeg
		if err := rows.Scan(&i.ID, &i.Label, &i.Rank); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const unionAllChained = `-- name: UnionAllChained :many
SELECT id, label, rank FROM not_null_leg
UNION ALL
SELECT id, label, rank FROM nullable_leg
UNION ALL
SELECT id, label, rank FROM not_null_leg
`

type UnionAllChainedRow struct {
	ID    pgtype.Int8
	Label pgtype.Text
	Rank  int32
}

// Chained UNION ALL — middle and right legs introduce nullability.
func (q *Queries) UnionAllChained(ctx context.Context) ([]UnionAllChainedRow, error) {
	rows, err := q.db.Query(ctx, unionAllChained)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []UnionAllChainedRow
	for rows.Next() {
		var i UnionAllChainedRow
		if err := rows.Scan(&i.ID, &i.Label, &i.Rank); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const unionAllMixed = `-- name: UnionAllMixed :many
SELECT id, label, rank FROM not_null_leg
UNION ALL
SELECT id, label, rank FROM nullable_leg
`

type UnionAllMixedRow struct {
	ID    pgtype.Int8
	Label pgtype.Text
	Rank  int32
}

// UNION ALL — column nullability is OR-merged across legs:
// left leg's `id`/`label` are NOT NULL, right leg's are nullable → result nullable.
// `rank` is NOT NULL on both sides → result NOT NULL.
func (q *Queries) UnionAllMixed(ctx context.Context) ([]UnionAllMixedRow, error) {
	rows, err := q.db.Query(ctx, unionAllMixed)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []UnionAllMixedRow
	for rows.Next() {
		var i UnionAllMixedRow
		if err := rows.Scan(&i.ID, &i.Label, &i.Rank); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const unionMixed = `-- name: UnionMixed :many
SELECT id, label, rank FROM not_null_leg
UNION
SELECT id, label, rank FROM nullable_leg
`

type UnionMixedRow struct {
	ID    pgtype.Int8
	Label pgtype.Text
	Rank  int32
}

// UNION (set-distinct) — same nullability merge rule applies.
func (q *Queries) UnionMixed(ctx context.Context) ([]UnionMixedRow, error) {
	rows, err := q.db.Query(ctx, unionMixed)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []UnionMixedRow
	for rows.Next() {
		var i UnionMixedRow
		if err := rows.Scan(&i.ID, &i.Label, &i.Rank); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
