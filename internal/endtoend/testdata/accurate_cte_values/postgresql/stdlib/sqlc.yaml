version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    database:
      uri: "postgresql://postgres:mysecretpassword@localhost:5432/postgres"
    gen:
      go:
        package: "querytest"
        out: "go"
    analyzer:
      accurate: true
