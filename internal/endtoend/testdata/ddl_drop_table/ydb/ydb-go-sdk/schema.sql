CREATE TABLE venues (
    hi Text,
    PRIMARY KEY (hi)
);
DROP TABLE venues;

CREATE TABLE Authors (
    id Int32,
    PRIMARY KEY (id)
);
DROP TABLE Authors;

CREATE TABLE "Books" (
    id Int32,
    PRIMARY KEY (id)
);
DROP TABLE "Books";
