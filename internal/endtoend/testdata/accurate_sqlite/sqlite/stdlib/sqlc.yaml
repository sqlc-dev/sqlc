version: "2"
sql:
  - engine: sqlite
    schema: "schema.sql"
    queries: "query.sql"
    database:
      managed: true
    analyzer:
      database: "only"
    gen:
      go:
        package: "querytest"
        out: "go"
