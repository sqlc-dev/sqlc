CREATE TABLE foo (
    other     text NOT NULL,
    total     bigint NOT NULL,
    retyped   text NOT NULL
);

CREATE TABLE bar (
    other          text NOT NULL,
    total          bigint NOT NULL,
    also_retyped   text NOT NULL
);

CREATE TABLE baz (
    other          text NOT NULL,
    total          bigint NOT NULL,
    also_retyped   text NOT NULL
);
