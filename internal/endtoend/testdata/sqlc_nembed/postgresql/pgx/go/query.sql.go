// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: query.sql

package querytest

import (
	"context"
	"database/sql"
)

const leftJoin = `-- name: LeftJoin :many
SELECT users.id, users.name, posts.id, posts.user_id, posts.body FROM users
LEFT JOIN posts ON users.id = posts.user_id
`

type LeftJoinRow struct {
	User User  `db:"user" json:"user"`
	Post *Post `db:"post" json:"post"`
}

func (q *Queries) LeftJoin(ctx context.Context) ([]LeftJoinRow, error) {
	rows, err := q.db.Query(ctx, leftJoin)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []LeftJoinRow
	for rows.Next() {
		var i LeftJoinRow
		var nembedPostID *int32
		var nembedPostUserID *int32
		var nembedPostBody *sql.NullString
		if err := rows.Scan(
			&i.User.ID,
			&i.User.Name,
			&nembedPostID,
			&nembedPostUserID,
			&nembedPostBody,
		); err != nil {
			return nil, err
		}
		if nembedPostID != nil || nembedPostUserID != nil || nembedPostBody != nil {
			i.Post = &Post{
				ID:     *nembedPostID,
				UserID: *nembedPostUserID,
				Body:   *nembedPostBody,
			}
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const leftJoinOne = `-- name: LeftJoinOne :one
SELECT users.id, users.name, posts.id, posts.user_id, posts.body FROM users
LEFT JOIN posts ON users.id = posts.user_id
LIMIT 1
`

type LeftJoinOneRow struct {
	User User  `db:"user" json:"user"`
	Post *Post `db:"post" json:"post"`
}

func (q *Queries) LeftJoinOne(ctx context.Context) (LeftJoinOneRow, error) {
	row := q.db.QueryRow(ctx, leftJoinOne)
	var i LeftJoinOneRow
	var nembedPostID *int32
	var nembedPostUserID *int32
	var nembedPostBody *sql.NullString
	err := row.Scan(
		&i.User.ID,
		&i.User.Name,
		&nembedPostID,
		&nembedPostUserID,
		&nembedPostBody,
	)
	if nembedPostID != nil || nembedPostUserID != nil || nembedPostBody != nil {
		i.Post = &Post{
			ID:     *nembedPostID,
			UserID: *nembedPostUserID,
			Body:   *nembedPostBody,
		}
	}
	return i, err
}

const nembedOnly = `-- name: NembedOnly :one
SELECT users.id, users.name FROM users WHERE id = $1
`

type NembedOnlyRow struct {
	User *User `db:"user" json:"user"`
}

func (q *Queries) NembedOnly(ctx context.Context, id int32) (NembedOnlyRow, error) {
	row := q.db.QueryRow(ctx, nembedOnly, id)
	var i NembedOnlyRow
	var nembedUserID *int32
	var nembedUserName *string
	err := row.Scan(&nembedUserID, &nembedUserName)
	if nembedUserID != nil || nembedUserName != nil {
		i.User = &User{
			ID:   *nembedUserID,
			Name: *nembedUserName,
		}
	}
	return i, err
}

const withAlias = `-- name: WithAlias :many
SELECT u.id, u.name, p.id, p.user_id, p.body FROM users u
LEFT JOIN posts p ON u.id = p.user_id
`

type WithAliasRow struct {
	User User  `db:"user" json:"user"`
	Post *Post `db:"post" json:"post"`
}

func (q *Queries) WithAlias(ctx context.Context) ([]WithAliasRow, error) {
	rows, err := q.db.Query(ctx, withAlias)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []WithAliasRow
	for rows.Next() {
		var i WithAliasRow
		var nembedPostID *int32
		var nembedPostUserID *int32
		var nembedPostBody *sql.NullString
		if err := rows.Scan(
			&i.User.ID,
			&i.User.Name,
			&nembedPostID,
			&nembedPostUserID,
			&nembedPostBody,
		); err != nil {
			return nil, err
		}
		if nembedPostID != nil || nembedPostUserID != nil || nembedPostBody != nil {
			i.Post = &Post{
				ID:     *nembedPostID,
				UserID: *nembedPostUserID,
				Body:   *nembedPostBody,
			}
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
