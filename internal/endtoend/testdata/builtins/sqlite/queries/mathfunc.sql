-- name: GetAcos :one
SELECT acos(1.0);

-- name: GetAcosh :one
SELECT acosh(1.0);

-- name: GetAsin :one
SELECT asin(1.0);

-- name: GetAsinh :one
SELECT asinh(1.0);

-- name: GetAtan :one
SELECT atan(1.0);

-- name: GetAtan2 :one
SELECT atan2(1.0, 0.5);

-- name: GetAtanh :one
SELECT atanh(1.0);

-- name: GetCeil :one
SELECT ceil(1.0);

-- name: GetCeilin :one
SELECT ceiling(1.0);

-- name: GetCos :one
SELECT cos(1.0);

-- name: GetCosh :one
SELECT cosh(1.0);

-- name: GetDegrees :one
SELECT degrees(1.0);

-- name: GetExp :one
SELECT exp(1.0);

-- name: GetFloor :one
SELECT floor(1.0);

-- name: GetLn :one
SELECT ln(1.0);

-- name: GetLog :one
SELECT log(1.0);

-- name: GetLog10 :one
SELECT log10(1.0);

-- name: GetLogBase :one
SELECT log(1.0, 2.0);

-- name: GetLog2 :one
SELECT log2(1.0);

-- name: GetMod :one
SELECT mod(1, 2);

-- name: GetPi :one
SELECT pi();

-- name: GetPow :one
SELECT pow(1, 2);

-- name: GetPower :one
SELECT power(1, 2);

-- name: GetRadians :one
SELECT radians(1);

-- name: GetSin :one
SELECT sin(1.0);

-- name: GetSinh :one
SELECT sinh(1.0);

-- name: GetSqrt :one
SELECT sqrt(1.0);

-- name: GetTan :one
SELECT tan(1.0);

-- name: GetTrunc :one
SELECT trunc(1.0);
