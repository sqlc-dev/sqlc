CREATE TABLE books (
                       id   integer PRIMARY KEY,
                       title text      NOT NULL,
                       author text     NOT NULL,
                       pages numeric      NOT NULL
);
