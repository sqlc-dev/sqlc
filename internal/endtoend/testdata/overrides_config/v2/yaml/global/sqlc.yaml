version: "2"
overrides:
  go:
    rename:
      one: "ONE"
    overrides:
      - db_type: "text"
        go_type: "config/global.Text"
      - column: "overrides.three"
        go_type:
          import: "config/global"
          type: "Three"
sql:
- schema: "../../../schema.sql"
  queries: "../../../query.sql"
  engine: "postgresql"
  gen:
    go: 
      out: "db"
